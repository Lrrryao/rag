1. 首先主要看和VectorRAG相关的，起码要先了解一下langchain的开发流程吧,最好这一步直接开始做一个项目，比如[How to Build a Production-Ready RAG AI Agent in Python](https://www.youtube.com/watch?v=AUQJ9eeP-Ls)
2. 然后再试试什么LightRAG和AgenticRAG,具体可以参考[RAG选择的考量](./RAG选择的考量.md)这个笔记. 关于agentic RAG,我觉得用它为基础，把LightRAG和AgenticRAG包装成为agent tool. 然后基于一个top agent来进行。

- 可以选择一个top agent作为manager, 然后使用lightRAG+VectorRAG作为医院侧的知识库，然后用VectorRAG作为用户侧的增强，最后再用一个search_engine_tool来适当检索图片并添加到report generation中(这个可以被top agent并行使用)，最后把拿到的内容由top agent来统一进行调度，并生成报告。同时，这个top agent还可以决定什么时候使用RAG，运用提示词技术压缩等。

- 当然不要把Agentic RAG 和 Chatbot Agent弄混，前者还是作为一个基础服务后者的

  
3. 可以开始设计这个向量数据库的后台结构了，我觉得可以把向量数据库作为一个默认选项，然后把知识图谱和Agentic RAG作为备选项吧
4. 然后可以再关注一下 多模态嵌入模型(embedding model) 但是这个似乎还没被投入使用。所以可以放到最后去看
5. 