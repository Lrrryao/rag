What are the differences in the query methods local, global, and question generation? 

**下一步该学一下langchain了**

###  对于VectorRAG来说，检索方式为：
1. 向量相似度检索,重点在于语义匹配
2. 关键字/词检索，重点在于关键词匹配，比如BM25搜索算法
3. 混合匹配(hybrid search)/多路召回(multi query):
    PS: 混合匹配和多路召回是一个意思
   - 在 RAG 检索场景中，首要目标是确保最相关的结果能够出现在候选列表中。向量检索和关键词检索各有其独特优势，混合检索通过结合这多种检索技术，弥补了各自的不足，提供了一种更加全面的搜索方案。
   - 仅具备混合检索的能力还不足以满足需求，检索到的结果还需要经过优化排序。重排序（Reranking）的目的是将混合检索的结果进行整合，并将与用户问题语义最契合的结果排在前列。在上述案例中，由于缺乏有效的排序，我们期望的结果位于第一和第四位，尽管依然可以被检索到，但理想情况下，如果检索方式更为精确，该结果应该被优先排序在前两位。

参考：[06 RAG检索：混合检索与重排序技术](https://uaxe.github.io/geektime-docs/AI-%E5%A4%A7%E6%95%B0%E6%8D%AE/RAG%E5%BF%AB%E9%80%9F%E5%BC%80%E5%8F%91%E5%AE%9E%E6%88%98/06-RAG%E6%A3%80%E7%B4%A2%EF%BC%9A%E6%B7%B7%E5%90%88%E6%A3%80%E7%B4%A2%E4%B8%8E%E9%87%8D%E6%8E%92%E5%BA%8F%E6%8A%80%E6%9C%AF/#_5)


### 对于GraphRAG来说，检索方式基于知识图片 KnowledgeGraph

GraphRAG出品自微软团队，但是缺点是太贵了，而且速度很慢。但是好处是在准确性、可解释性和处理复杂关系方面更优

详情看[全方面对比GraphRAG和LightRAG](https://github.com/NanGePlus/LightRAGTest)

![](../img/lightVSgraph.png)

#### 基于知识图谱的RAG检索方式分别为

1. local search method 
2. global search method
3. hybrid search method
4. naive search method (但是这种大概用不到吧)


#### 重排序(re-ranking)：在检索拿到的结果里进行重排序

